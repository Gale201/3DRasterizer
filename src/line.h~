#ifndef LINE_H
#define LINE_H

#include "renderer.h"

void DrawLine(int x0, int y0, int x1, int y1, uint32_t color);
void DrawDepthLine(int x0, int y0, float z0, int x1, int y1, float z1, uint32_t color);
void DrawHorizontalLineDepth(int x0, int x1, int y, float z0, float z1, uint32_t color);
void DrawHorizontalLineDepth2(float x0, float x1, int y, float inv_z0, float inv_z1, uint32_t color);

#endif
