#include "clock.h"

Clock CreateClock(void)
{
	float lastTicks = SDL_GetTicks();
	return (Clock) { lastTicks, 0, 0 };
}

void TickClock(Clock* clock)
{
	float currentTicks = SDL_GetTicks();
	clock->deltaTime = (currentTicks - clock->lastTicks) / 1000;
	clock->lastTicks = currentTicks;
}
